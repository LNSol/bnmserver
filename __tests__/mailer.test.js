import { jest } from '@jest/globals';
import request from 'supertest';

/**
 * send: token, emailTitle, emailbody(userName, button with token and userId)
 * apply: check token by user
 */
describe('mailsender', () => {
  test('send mail', () => {

  });

  test('confirm token', () => {

  });
});
